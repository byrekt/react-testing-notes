import fibonacci from './Lesson1'

describe('fibonacci finder', () => {
  // Just forces suite failure if you haven't added any assertions yet. You wouldn't have to do this in
  // an actual test suite
  expect.hasAssertions()

  it('returns the 54th fibonacci number', () => {
    // HINT: the 54th fibonacci number is 139583862445

  })
  it('returns 1 for the 2nd fibonacci number', () => {

  })
  it('returns -1 if the input is less than 0', () => {

  })
  it('returns -1 if the input is not a number', () => {

  })
  it('returns -1 if the input is not an integer', () => {

  })
})


















































































// describe('fibonacci finder', () => {
//   expect.hasAssertions()
//   it('finds the 105th fibonacci number', () => {
//     // expect(fibonacci(5)).toBe(8)
//   })
//   it('returns 1 for the 2nd fibonacci number', () => {
//     // expect(fibonacci(2)).toBe(1)
//   })
//   it('returns -1 if the input is less than 0', () => {
//     // expect(fibonacci(-4)).toBe(-1)
//   })
//   it('returns -1 if the input is not a number', () => {
//     // expect(fibonacci("four")).toBe(-1)
//   })
//   it('returns -1 if the input is not an integer', () => {
//     // expect(fibonacci(4.7)).toBe(-1)
//   })
// })